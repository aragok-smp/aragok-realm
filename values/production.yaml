version: 1

values: {}

targets:
  proxy:
    values:
      grade: production
      memory:
        initial: 1G
        max: 1G
  survival:
    values:
      memory:
        initial: 4096M
        max: 8192M
