version: 1

name: PaperMC Survival Additions
description: >-
  Adds survival-specific stuff (like difficulty, plugins, ...)

maintainers:
  - name: darmiel

imports:
  values:
    "aragokPlugin.version":
      description: "Version of aragok-plugin"
      required: true
    "aragokPlugin.checksums.survival":
      description: "Checksum of the survival aragok-plugin"
      required: true
