# SOPS?
# stellen wir uns vor, die Datei ist verschlüsselt mit SOPS
databases:
  redis:
    host: 127.0.0.1
    port: 6379
    password: your_redis_password

  postgres:
    host: 127.0.0.1
    port: 5432
    username: !env POSTGRES_USER
    password: !env POSTGRES_PASSWORD
