version: 1

name: proxy Overlay

imports:
  targets:
    survival:
      description: "Retrieve Server Bindings from Manifest"
      values:
        bindIP:
          description: "IP from survival server"
          required: true
        bindPort:
          description: "Port from survival server"
          required: true
