version: 1

name: proxy Overlay

imports:
  targets:
    survival:
      description: "Retrieve Server Bindings from Manifest"
      values:
        bindPort:
          description: "Port from survival server"
          required: true
        serverName:
          description: "Service name from survival server"
          required: true
